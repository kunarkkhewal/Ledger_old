const expenses = require('./model'); //Model import

const getExpenses = () => {
    return "getExpenses"
}

const addExpenses = () => {
    return "addExpenses"
}

module.exports = {getExpenses, addExpenses}